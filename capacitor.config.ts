import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rainyday.app',
  appName: 'RainyDays',
  webDir: 'out',
  plugins: {}
};

export default config;
